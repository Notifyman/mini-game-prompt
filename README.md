任务选择总控：
在开始执行任何任务之前，必须先暂停并询问用户要处理哪个任务，不得直接进入任务一、任务二、任务三、任务四或任务五。

你必须先回复：

请选择本次要处理的任务：
1. 任务一：小游戏素材图片国际化处理
2. 任务二：首页增加“免广告”隐藏按钮，并全局跳过真实广告
3. 任务三：TikTok 小游戏桌面增加引导按钮
4. 任务四：把游戏内 UI 文案和 toast 文案改成英语
5. 任务五：保证编译后的主包小于 4M
6. 任务六：集成 tiktok 激励广告

你现在应该回复：`1`、`2`、`3`、`4` 或 `5`

规则：
- 只有用户明确选择某一个任务后，才能开始执行对应任务。
- 如果用户同时选择多个任务，必须询问先处理哪一个，不要并行开始。
- 如果用户回复内容不明确，必须继续追问任务编号或任务名称。
- 进入某个任务后，只执行该任务对应的规则和步骤，不要自动切换到其他任务。
- 当前任务完成后，如果用户没有明确要求继续其他任务，必须停止并询问是否选择下一个任务。
- 这个是 tiktok 小游戏项目，不是抖音小游戏，也不是微信小游戏

任务一：
以上小游戏素材图片需要做国际化处理，把中文文案改成英文。

工作模式：半自动批处理 + 多段人工审核 + 多画布连续处理。
外部 AI 只负责抹除中文；Codex 负责裁剪、恢复透明、生成英文透明文字层、创建审核 job、最终合成写回。

任务步骤：
1.图片根据字体样式分组
2.生成画布图用于抹除文字
3.裁剪分割图片用于审核无文字图
4.等待调整文字 frame
5.拼接背景图和文字图
6.结果图替换到小游戏项目中

额外安装位置规则：
- 所有额外安装、克隆、创建的工具项目，必须统一安装到 `~/` 路径下。
- 不要把额外工具项目安装到小游戏项目目录、小游戏项目父目录、node_modules、library、build、temp 或当前批次目录里。
- 后续批次必须复用 `~/` 下已有工具和 venv。

禁止：
- 不要直接在原图上本地涂抹、补色、inpaint 或用普通字体重绘中文区域。
- 未经审核，不要把英文写回资源。
- 未经样式分组审核，不要进入外部抹除画布生成。
- 未经 cleaned base 审核，不要创建 i18n-image-reviewer job。
- 不允许把外部回传图直接套原图 alpha 后当最终底图。
- 不允许把外部画布背景色、白底、灰底、矩形块带进 cleaned base 或 final。
- 不允许使用 SVG 作为审核或最终合成文字层；审核和最终合成文字层必须是透明 PNG。
- 不允许遮挡或重画 AD、播放图标、金币、房子、道具图标等元素。
- 第零步禁止搜索项目引用、场景文件、脚本文件、prefab、library、build、temp、node_modules。
- 第零步禁止为了确认中文语义去查代码上下文；中文含义不确定时先写“待确认”。
- 第零步禁止生成多套专项放大检查图，例如 white inspect、black inspect、contrast inspect、4x zoom 等；除非用户明确要求。
- 第零步禁止把分组判断做成高置信度研究任务；宁可先输出可审核的候选分组，也不要为了合组反复确认。

效率要求：
- 只检查当前任务相关图片文件，不要全仓库大范围扫描旧项目、旧批次、node_modules。
- 第零步只能读取待处理图片本身，不得搜索项目引用、library、build、场景文件、脚本文件或生成目录。
- 同一份工具说明、job 格式、运行时目录检查只读取一次，后续复用结论；第零步不需要读取 reviewer README。
- 每个阶段尽量用一个脚本批量完成，不要逐张手写大量重复命令。
- 第零步必须快速完成：目标 5 分钟内输出粗分组审核结果；如果 5 分钟内无法完成，必须先输出当前候选分组给用户审核，不要继续深挖。
- 第零步辅助图最多生成：一张源图总览图、每个样式组一张分组审核画布、必要时最多一张全局放大辅助图。
- 不要为白字、黑字、多行文本、按钮文字分别生成额外检查图，除非用户要求。
- 中文含义和英文翻译可以先基于文件名和可见文字粗填；不确定时写“待确认”，不要在第零步查代码。
- montage 只生成必要版本；不要反复生成整批大长图。问题定位时只看问题编号。
- 第二步必须先跑自动 alpha gate，再给人看图；不要靠肉眼先放行。
- 1024x1024 外部抹除画布应尽可能多地排列当前批次的小素材图；除非素材过大或间距/编号会冲突，不要只放少量小图导致画布浪费。
- 输出给用户查看的图片、manifest、report、montage、job 目录等路径，必须尽量用 Markdown 超链接格式，方便快速点击审核。
- 每次暂停或阶段结束时，必须明显告诉我“你现在应该回复什么内容进行下一步”。
- 每个 1024x1024 画布对应的素材完成最终写回后，Codex 应继续从剩余待处理图片列表中生成下一个 1024x1024 外部抹除画布，并再次暂停等待外部 AI 回传，直到整批素材全部处理完成。

工具准备：
- 英文透明文字层必须优先使用 Pillow 绘制。
- 如果当前 Python 环境没有 Pillow，不要在每个小游戏项目里重复安装。
- Pillow 依赖必须安装到 `~/` 路径下的共享 venv，例如：
  `python3 -m venv ~/.venv-i18n-image-tools`
  `~/.venv-i18n-image-tools/bin/python -m pip install -U pip pillow`
- 后续批次复用 `~/.venv-i18n-image-tools`。
- 不要把 Pillow 安装到 node_modules，也不要为每个小游戏项目或每个批次重复创建一套依赖。

本地审核工具：
- 本地审核工具统一使用 `~/i18n-image-reviewer`。
- 如果找不到 `~/i18n-image-reviewer`，从以下仓库拉取：
  `https://github.com/Notifyman/i18n-image-reviewer`
- 必须克隆到：
  `git clone https://github.com/Notifyman/i18n-image-reviewer ~/i18n-image-reviewer`
- 不要克隆到小游戏项目目录、小游戏项目父目录或当前批次目录。
- 如果审核工具缺少 npm 依赖，只能在 `~/i18n-image-reviewer` 内安装，不要在小游戏项目里安装。
- 不要在第零步读取 reviewer README 或检查服务状态。
- 只有准备创建 reviewer job 前，才读取该仓库 README，一次即可，后续复用结论。
- 当前 reviewer 支持同一张图多个文字块单独选择、拖拽、滚轮缩放、方向键微调。
- 保存结果以 `layerTransforms` 为权威数据。
- 新任务建议直接写 `textLayers`。
- 旧的单张透明文字层会按 alpha 自动拆出明显分离的文字块。
- 具体 job 格式以该仓库 README 为准。

审核地址：
`http://127.0.0.1:5178/jobs/<jobId>`

这个 jobId 用 `{游戏名}_{yyyyMMDDHHMMSS}` 这样的值。

如果服务没启动，可以启动：
`cd ~/i18n-image-reviewer && npm start`

核心原则：
1. 外部 AI 只负责抹除中文。
2. Codex 负责裁剪、恢复透明、生成英文透明 PNG 文字层、创建审核 job、最终合成写回。
3. 1024x1024 外部画布优先使用透明 PNG；如果必须用纯色背景，必须在 manifest 记录背景色，且该背景色绝不能进入 cleaned base/final。
4. 文字层默认生成 5 倍尺寸透明 PNG，例如逻辑尺寸 106x42，实际 PNG 为 530x210，并在 job.json 的 text layer 里写 `assetScale: 5`。
5. 如果原图 alpha 包含中文文字，不能整张复用原 alpha；必须清除旧中文 alpha。
6. 如果原图透明区、半透明阴影、圆角、边缘被外部画布污染，必须恢复干净 alpha。
7. 如果外部 AI 误删 AD、播放图标、金币、房子、道具图标等，必须从原图贴回对应区域，不能重画。
8. 样式分组的判断标准必须以“文字主体的字体颜色 + 字体样式 + 字号/文字视觉高度”一致为优先级；路径规律、同目录、同前缀、同 UI 系列只能作为辅助依据，不能单独决定合组。
9. 分组时必须先识别文字主体像素，不要把按钮底色、装饰图标色、整张素材主色、透明棋盘预览背景色当成文字颜色。
10. 分组审核画布与外部抹除画布是两类不同画布：
   - 分组审核画布：用于让用户确认哪些素材可共用英文文字样式，必须按样式组拆成多张小画布，每张画布只放同字体颜色、同字体样式、同字号/视觉高度的素材。
   - 外部抹除画布：用于交给外部 AI 抹除中文，应优先考虑 1024x1024 利用率，尽可能多排列当前批次小素材。
11. 不允许因为素材都在同目录、同前缀、同按钮组，就把字体颜色、字号或视觉高度明显不同的素材放进同一文字样式组。例如白字、纯黑字、深黑蓝字、青绿色字必须拆开；同色但字号高度明显不同也必须拆开。
12. 也不要把“保守拆组”理解成逐图拆分：如果文字主体颜色、描边/阴影、字体厚重程度和视觉高度一致，即使文件名、目录或具体文案不同，也应该合为同一个候选样式组供用户审核。
13. 按钮、图标、面板、带鱼/金币/AD/播放等非文字元素的素材，如果文字样式一致但承载方式不同，可以合组但必须标记保护区域和例外；如果承载方式会影响英文文字绘制或遮挡风险，则单独拆组。
14. 多文字模块图片必须标记为多 textLayers，不要和普通单文字图片混为一组。

第零步：快速样式分组审核并暂停
目标：只做样式分组，不做语义深挖，不查项目引用，不进入外部抹除画布生成。

允许做的事情仅限：
1. 读取待处理图片列表，并按列表顺序完整编号；不要漏图，不要自行假定总数。
2. 用一个批处理脚本统计每张图的尺寸、alpha bbox、高 alpha bbox、文字主体色、描边色、阴影色、有效文字高度。
3. 生成一张源图总览图。
4. 基于“文字主体字体颜色 + 字体样式 + 字号/文字视觉高度”拆分候选样式组。
5. 为每个样式组生成一张分组审核画布。
6. 输出分组表、分组总览、分组 JSON/manifest，并暂停等待用户审核。

第零步分组方法：
- 先看可见文字本身，而不是整张素材的主色。
- 像素统计应优先参考高 alpha 文字区域、文字 bbox、主文字色、描边/阴影色和视觉高度。
- 对按钮、面板、图标类图片，必须区分“按钮/面板底色”和“文字主体色”；底色不能作为文字颜色分组依据。
- 对白字、纯黑字、深黑蓝字、青绿色字、蓝描边白字等颜色体系必须分开。
- 对同色同样式但字号高度相近的素材，应合为候选样式组。例如同一批深黑蓝厚重标签、同一批纯黑厚重标签、同一批提示正文单行/两行文字。
- 对同色同样式但视觉高度明显不同的素材，应拆组。例如小标签、大标签、单行正文、两行正文、大按钮文字。
- 对多行正文，应按文字整体高度和行数拆组：单行长文、两行中版、两行大版不要混组。
- 对单字计数/标题碎片，如果颜色、字重和视觉高度一致，可以合组；如果字高明显不同则拆组。
- 对多文字模块，例如大标题 + 小副标题、玩法说明双行但带图标保护区，应单独成组并标记后续使用多个 textLayers。
- 如果不确定是否能合组，优先输出候选分组给用户审核，不要反复做专项检查图。

明确禁止：
- 禁止搜索项目引用、场景文件、脚本文件、prefab、library、build、temp、node_modules。
- 禁止为了确认 Text_fish1/Text_fish2 这类上下文去查代码；中文含义不确定时写“待确认”。
- 禁止生成多套专项检查图；最多一张源图总览 + 每组一张审核画布 + 必要时一张全局放大辅助图。
- 禁止为了合并分组反复放大检查。
- 禁止在第零步读取 reviewer README 或检查审核工具运行状态。

分组硬规则：
- 只有“文字主体字体颜色 + 字体样式 + 字号/文字视觉高度”都一致的图片，才能放进同一个样式组。
- 同路径规律但文字主体颜色不同，必须拆组。
- 同颜色但字号/视觉高度明显不同，必须拆组。
- 同颜色同字号但按钮底图、图标保护区域或文字承载方式不同，必须至少标为例外项；如影响英文绘制风险，应单独拆组。
- 多文字模块图片必须标记为多 textLayers，不要和普通单文字图片混为一组。
- 不要因为图片整体 dominant color 相同就合组，也不要因为整图 dominant color 不同就拆开；以文字主体像素为准。
- 不要过度逐图拆分：明显同一文字样式的素材应合成候选组，让用户审核确认。

分组审核画布规则：
- 分组审核画布不是外部抹除画布。
- 每张分组审核画布只展示一个样式组。
- 每张画布上可以放同组多张素材，但不能混入文字主体颜色、字体样式、字号明显不一致的素材。
- 同时生成一个分组总览图，方便快速检查所有组。
- 分组审核画布应显示编号、文件名、中文含义、建议英文和样式说明；但不要生成多套专项放大检查图。

输出“字体样式/字号分组审核表”，至少包含：
- 分组名
- 编号
- 原文件路径
- 中文含义；不确定写“待确认”
- 建议英文；不确定写“待确认”
- 判断依据：视觉相似 / 像素统计 / 路径辅助
- 建议共用的字体颜色、字体风格、字号/文字视觉高度、描边、阴影、颜色体系
- 分组审核画布路径
- 需要单独处理的例外项

时间盒：
- 第零步目标 5 分钟内完成。
- 如果 5 分钟内无法完成，必须立即输出当前候选分组给用户审核，不要继续检查。

到这里必须暂停，等我审核分组。
明确告诉我：
- 如果分组正确，请回复：`样式分组通过`
- 如果需要调整，请回复：`调整分组：...`

第一步：生成外部抹除用画布并暂停
- 只有我回复 `样式分组通过` 后才能进入这一步。
- 注意：这里生成的是“外部抹除画布”，不是第零步的“分组审核画布”。
- 外部抹除画布不需要按字体样式分组排列；它的目标是提高 1024x1024 画布利用率。
- 从待处理图片列表中取一批图片，尽可能多地排列小素材图，在不压编号、不互相遮挡、不影响外部 AI 抹除、不遮挡 AD/图标/按钮元素的前提下提高画布利用率。
- 按原始尺寸无缩放排布到 1024x1024 画布上，优先透明背景；如用纯色背景，记录颜色。
- 素材之间留足间距。
- 编号标签 01、02、03 必须放在素材外，不能压到素材本体。
- 生成 manifest，记录：编号、原路径、尺寸、画布坐标、中文含义、建议英文、AD/图标保留要求、原文案区域、建议英文区域、素材风格、字体样式分组、画布背景色。
- 到这里必须暂停。
- 告诉我画布路径、manifest 路径、本批编号和原文件；图片和文件路径尽量输出为 Markdown 超链接，让我能直接点击查看。
- 让我用外部 AI 抹除中文后回传同一张 1024x1024 画布。
- 明确告诉我：
  - 外部 AI 抹除完成后，请回复：`已抹除，回传画布路径：<路径>`

第二步：裁剪 erased crop，生成 cleaned base，并先给我审核
收到回传画布后：
- 先检查回传画布尺寸是否仍是 1024x1024。
- 按 manifest 坐标裁剪每个素材。
- 对每张图分别判断透明恢复策略：
  - 原图 alpha 不含中文：可用原图 alpha 恢复透明边界。
  - 原图 alpha 含中文：必须清理旧中文 alpha，不能整张复用。
  - 外部图是纯色/白底/灰底：必须基于原图 alpha、主体边界、阴影范围恢复透明，不能让背景进入 base。
  - 按钮/面板/图标类素材：外部 AI 擦除造成的平色块、矩形块、灰底、白底不能进入 base。
- 恢复原图 alpha 时，避免会重置 alpha 的错误命令。ImageMagick 推荐形式：
  `magick erased.png \( original.png -alpha extract \) -compose CopyOpacity -composite PNG32:base.png`
  不要在 `CopyOpacity` 前随意加 `-alpha on`。

第二步 alpha 硬门禁：
- 创建 reviewer job 前，必须为每张 cleaned base 生成 alpha report。
- report 至少包含：原图尺寸/channels、base 尺寸/channels、四角 RGBA、若干边缘/阴影点 RGBA、原透明区是否仍透明。
- 如果原图某点 alpha=0，cleaned base 同点必须 alpha=0。
- 如果原图边缘是半透明阴影，cleaned base 必须保留半透明，不能变全不透明。
- 任一素材出现白底、灰底、纯色底、矩形块、原透明角落变不透明，必须返工，不能进入人工审核。

第二步输出：
- original 原图参考
- erased crop 抹除裁剪
- cleaned base over checkerboard 透明底预览
- alpha report
- 必要时额外输出 original alpha vs cleaned base alpha 对比图
- 所有输出图片、report、montage 路径都尽量用 Markdown 超链接展示，方便我快速打开审核。
- 到这里暂停，等我回复通过或指出编号继续调整。
- 未经我确认，不要创建 i18n-image-reviewer job。
- 明确告诉我：
  - 如果 cleaned base 通过，请回复：`cleaned base 通过`
  - 如果需要修复，请回复：`修复编号：...`

第三步：生成英文透明文字层并创建 i18n-image-reviewer job
我确认 cleaned base 通过后：
- 为每张图生成英文透明 PNG 文字层，默认 5 倍尺寸。
- 英文透明文字层必须使用 Pillow 绘制；如果 Pillow 不可用，按“工具准备”安装到 `~/` 路径下的共享 venv。
- 不要用 SVG 作为中间审核产物或最终文字层；Pillow 输出必须是透明 PNG。
- 每张图按已审核的字体样式分组生成英文层。
- 同一分组内应复用相同字体样式、字号高度、字重、字体宽窄、描边厚度、描边颜色、阴影方向、阴影距离、阴影透明度、颜色体系和视觉中心。
- 分组内如某张图空间不足，可微调字号或换行，但必须记录为例外。
- 不允许整批统一套一种字体样式。
- 不允许“盖色块 + 普通字体”。
- 不允许遮挡 AD、播放图标、金币、房子、道具图标等。
- 如果一张图片里有多个独立文字模块，应为每个模块生成独立透明 PNG 或独立 text layer，并写入 `textLayers`。
- 生成初始合成 preview，但 preview 只用于审核定位，不写回资源。
- 如果第三步才发现中文含义或英文建议不准确，可以在 job.json 中记录修正，不需要回到第零步重做样式分组，除非样式组本身错误。

创建目录：
`~/i18n-image-reviewer/jobs/<jobId>/`

目录结构：
`job.json`
`original/01.png`
`base/01.png`
`text/01_title@5x.png`
`text/01_caption@5x.png`
`preview/01.png`
`review.json` 由网页审核工具自动生成

job.json 必须记录：
- job id
- projectRoot
- batch
- sourceManifest
- createdAt
- items[]
- id
- assetPath
- originalPath
- basePath
- suggestedEn
- sourceMeaningZh
- styleType / style_notes
- textLayers[]

每个 textLayers[] 必须记录：
- id
- label
- path
- assetScale，例如 5
- rect: { x, y, width, height }，使用底图逻辑坐标，不使用 5 倍 PNG 像素坐标
- initialTransform: { x, y, scale }

旧字段 `textLayerPath`、`textLayerAssetScale`、`textLayerRect` 只作为兼容字段，不作为新任务首选格式。

第四步：网页审核并暂停
生成 job 后必须暂停。
告诉我：
- jobId
- 审核 URL：`http://127.0.0.1:5178/jobs/<jobId>`
- job 目录路径
- 初始 preview montage 路径
- 本批编号和原文件
- job 目录、初始 preview montage、关键预览图路径都用 Markdown 超链接输出
- 请我在网页里逐个文字块拖动/缩放，逐张点击“审核通过”，最后点击“全部审核通过”
- 审核完成后回到 Codex 回复：`继续处理`
- 明确告诉我：
  - 网页审核完成后，请回复：`继续处理`

第五步：读取审核结果，合成并写回游戏项目
我回复继续处理后：
- 读取 `~/i18n-image-reviewer/jobs/<jobId>/review.json`。
- 必须确认 `allApproved: true`，或每个 item 都 `approved: true`。
- 如果有未通过项，停止并列出编号。
- 合成时以 `layerTransforms` 为权威数据。
- 如果旧 job 没有 `layerTransforms`，才 fallback 到 item 顶层 `x`、`y`、`scale`。
- 按每个 text layer 的 x、y、scale 合成到 base。
- 合成前先生成 final preview 和 preview montage。
- 用 view_image 检查：英文对齐、偏移、遮挡、白底、灰底、矩形块、脏边、透明错误、旧中文残留。
- 再跑最终 alpha gate：最终图尺寸、channels、四角/边缘 alpha 必须符合原图透明结构；对于文字自身 alpha 变化，应允许文字区域内新增英文 alpha，但不能污染文字区域外原透明区。
- 预览和 alpha gate 都通过后，才写回 assets 原文件路径。
- 写回前备份原 assets。
- 用 `magick identify` 检查尺寸和 alpha/channels。
- 如果是 Cocos Creator / 小游戏项目，检查 `.meta` texture uuid，并同步已有运行时资源：
  - `library/imports/<uuid前两位>/<uuid>.png`
  - `build/.../native/<uuid前两位>/<uuid>.png`
- 不要运行用户禁止的构建命令。
- 如果没有 `library/imports` 或 `build/**/native`，记录“无运行时副本可同步”，不要反复扫描。
- 同步后生成 assets vs runtime 对比图，确认运行时资源也变成英文。
- 最终 preview montage、final report、备份目录、写回资源列表等路径必须尽量用 Markdown 超链接输出。
- 输出处理进度，报错已处理的图片路径和待处理的图片路径。

连续处理规则：
- 每个 1024x1024 画布内的素材完成第五步并写回后，Codex 不应默认结束整批任务。
- 如果待处理图片列表中还有未处理素材，Codex 应继续回到第零步或第一步：
  - 如果剩余素材的字体样式分组尚未审核，先回到第零步。
  - 如果分组已审核，直接生成下一个 1024x1024 外部抹除画布。
- 新画布仍需尽可能多地排列剩余小素材，并重新生成 manifest。
- 生成下一个画布后必须暂停，等待我用外部 AI 抹除中文并回传。
- 只有当待处理图片列表全部完成，才汇总说明整批完成。
- 每次暂停时都必须明确告诉我下一步应该回复什么。

暂停规则：
1. 样式分组表和分组审核画布生成后暂停，等我回复 `样式分组通过`。
2. 生成 1024x1024 外部抹除画布后暂停，等我回复 `已抹除，回传画布路径：<路径>`。
3. 收到抹除画布并裁出 cleaned base、通过 alpha gate、生成预览后暂停，等我回复 `cleaned base 通过`。
4. 创建 i18n-image-reviewer job 后暂停，等我网页审核并回复 `继续处理`。
5. 未经我确认，不要最终合成、写回 assets 或同步运行时资源。
6. 单个画布完成写回后，如果还有剩余素材，继续生成下一个画布并在对应步骤暂停。
7. 每次任务处理结束或阶段暂停时，必须明显告诉我“你现在应该回复：...” 。

完成标准：
- 样式/字号/颜色分组已由我审核通过。
- 第零步已生成分组总览图和每个样式组单独的分组审核画布。
- manifest 中本批所有素材都处理完成。
- cleaned base 无中文残留、无白底/灰底、无矩形脏块、透明正常。
- alpha report 通过。
- 所有文字层为透明 5 倍 PNG。
- 多文字模块图片已使用 `textLayers`，并以 `layerTransforms` 作为审核结果。
- review.json 全部 approved。
- 最终图片按 review.json 合成。
- final preview 和 final alpha gate 通过。
- assets 已写回英文。
- Cocos library/imports 和已有 build/native 已同步；如不存在，已记录无需同步。
- 尺寸、channels、alpha 正常。
- 抽查无中文、无白底、无灰底、无脏边、无错位。
- 当前画布完成后，若仍有剩余待处理素材，已生成下一张外部抹除画布并暂停等待回传。
- 全部待处理素材都完成后，输出最终汇总，并明确说明无需继续回复。


任务二：

在这个小游戏首页增加一个“免广告”按钮。

如果首页有 logo，则点击 logo 触发。 如果没有 logo 则点击首页空白处触发。一秒内连续点击 5 次才展示出来。
按钮支持点击切换状态：开启时toast显示“免广告: 开”，关闭时toast显示“免广告: 关”。  
仅做 toast 提示，不要在游戏里展示“免广告”或任何相关的 label 或 UI，防止遮挡游戏画面。
当免广告开启后，游戏内所有真实广告入口都要被跳过，不只是首页开始按钮，包括激励视频、继续流程广告、宝箱广告等，都要走免广告逻辑并直接回调成功。  
实现时请把判断接到统一广告入口上，而不是只改某一个按钮入口，确保对游戏中实际广告功能全局生效。

任务三：

给当前小游戏首页增加两个 TikTok 引导按钮：

1. 添加到桌面引导按钮
2. 从侧边栏打开引导按钮

如果本地 `~/home-buttons` 已经存在 `https://github.com/Notifyman/home-buttons` 项目，则直接读取；如果不存在，则先拉取该项目。先完整阅读 `home-buttons` 里的接入说明和参考实现，再开始改当前小游戏。

要求：
- 两个按钮只允许出现在首页，不要加到游戏页、结算页、暂停页、设置页或其他页面。
- 按钮位置不能遮挡首页已有按钮、标题、Logo、关卡入口、设置、排行等可点击元素。
- 优先沿用当前项目已有首页节点、层级、资源加载和按钮绑定风格。
- 弹窗确认按钮必须真实可点击，不能只绑定 `"click"`；在 Cocos 里同时绑定 `cc.Node.EventType.TOUCH_END` 和 `"click"`，并加 300ms 防重复触发。
- 参考 `home-buttons` 和 FishyRescue 的完整 SDK 调用链，不要只实现单一路径：
  - 桌面：优先 `adUtils.executeAdditionDesktop/isAdditionDesktop`，再兼容 `tt.addShortcut` / 平台封装的 `addShortcut`，成功后尝试领取 shortcut mission reward。
  - 侧边栏：优先 `adUtils.startEntranceMission`，再兼容 `TTMinis` / `TTMinis.game.startEntranceMission`，再兼容 `executeShowSideBar/showSideBar/executeShowSidebar/showSidebar`，最后 fallback 到 `tt.navigateToScene({ scene: "sidebar" })`。
  - 查找 SDK 时同时检查全局变量、`window` 上的变量，以及项目已有的平台封装，避免只查一个对象导致 `sidebar sdk unavailable`。
- 弹窗关闭、确认后要正确销毁弹窗；侧边栏任务成功后隐藏侧边栏按钮并写入本地状态。
- 不要写单元测试。
- 不要运行 Taro build，也不要用 Taro 命令验证。
- 不要主动修改 `build/`、`temp/`、`library/` 等生成产物，除非用户明确要求。优先改源码。
- 修改后做静态检查：确认只改了首页相关源码、按钮绑定包含 `TOUCH_END`、没有调试残留、没有把按钮挂到首页以外。
- 如果用户是在已有 `build/bytedance` 目录里测试，提醒源码改动需要由 Cocos 重新编译/出包后才会进入构建产物。

任务四：

把这个游戏里的文案改成英语，包括 UI 文案和 toast 文案，不要有遗漏。

任务五：

保证编译后的主包小于 4M，注意不要直接改构建后的产物

任务六：

集成 tiktok 激励广告，如果我没有提供激励广告 id，则让我提供
